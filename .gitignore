# personal drafts never live in this repo
*.wg

# python bytecode (crt-theme is a script, not a package)
__pycache__/
*.pyc

# test + coverage artifacts
.pytest_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
